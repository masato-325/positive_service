<!DOCTYPE html>
<html>
  <head>
    <title>PositiveService</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <%= javascript_importmap_tags %>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-HBC1EY583E"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-HBC1EY583E');
    </script>

  </head>
  <!-- Google tag (gtag.js) -->


  <body>
    <% if logged_in? %>
      <%= render 'layouts/login_header' %>
    <% else %>
      <%= render 'layouts/header' %>
    <% end %>
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= bootstrap_class_for(message_type) %>" role="alert">
        <%= message %>
      </div>
    <% end %>
    <div class="back-image"> 
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
  </body>

</html>
